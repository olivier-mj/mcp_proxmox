# --- PROXMOX CONFIGURATION ---

# URL complète de l'interface Proxmox (avec port, généralement 8006)
PROXMOX_URL=https://192.168.1.100:8006

# Utilisateur Proxmox (généralement root@pam)
PROXMOX_USER=root@pam

# ID du Token API (créé dans Datacenter > Permissions > API Tokens)
# Note: Mettez SEULEMENT le nom du token (ex: mcp), pas l'ID complet avec l'utilisateur.
PROXMOX_TOKEN_ID=mon_nom_de_token

# Secret du Token API (affiché une seule fois lors de la création)
PROXMOX_TOKEN_SECRET=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Vérification SSL
# Mettre à 'false' si vous utilisez un certificat auto-signé (cas par défaut)
# Mettre à 'true' si vous avez un certificat valide (Let's Encrypt, etc.)
PROXMOX_VERIFY_SSL=false
